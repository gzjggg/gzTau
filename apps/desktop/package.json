{
  "name": "tau-desktop",
  "private": true,
  "version": "0.2.0",
  "description": "Tau desktop shell — bundled public UI + loopback Tau API (product repo only)",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  }
}
